# SLICE-SPEC — SAU_IMP (Impedimento do Profissional)

> Fields/types derived from SQL cursors in `sau_imp_impl.java` (inner class `sau_imp__default`,
> lines 3039-3063) and Java attribute declarations — **unverified against the live DB**
> (`gx-schema-mapper` could not reach the live PostgreSQL instance). Confirm column nullability
> and PK generation mechanism before marking beyond `specced`.

```yaml
slice: SAU_IMP
domain: impedimento
description: "Impedimento do Profissional"
wave: 2
complexity: M    # sau_imp_impl.java 159 KB; hwwsau_imp_impl.java 128 KB
primary_table: SAU_IMP

constellation:
  transaction:  sau_imp.java             # 1.4 KB servlet stub
  impl:         sau_imp_impl.java        # 159 KB — primary rule source
  bc:           null                     # no BC object
  workwith:     hwwsau_imp.java          # 1.3 KB stub
  workwith_impl: hwwsau_imp_impl.java    # 128 KB — list/search/filter logic
  view:         hviewsau_imp.java        # 1.3 KB stub (read-only detail)
  view_impl:    hviewsau_imp_impl.java   # 56 KB
  sections:
    - hsau_impgeneral.java               # 1.3 KB stub (general section panel)
    - hsau_impgeneral_impl.java          # 64 KB
  prompts:      []                       # no hprompt* for this slice; lookups come from SAU_PRO/SAU_PROESP
  sdt:          null

# SAU_PROESP / SAU_PRO are Wave-4 deps — FK kept as raw id column during Wave-2 cutover.
# SAU_ESP is already migrated (Wave-2 reference slice) — @ManyToOne safe.
# Wave-0 cross-cutting (auth + audit) required by all slices.
depends_on:
  - SAU_ESP        # EspCod FK; already migrated ✅
  - SAU_PRO        # ProPesCod FK; Wave 4 — omit DB-level FK until SAU_PRO is live
  - SAU_PROESP     # (ProPesCod, EspCod) pair existence check (service rule, not DB FK)
  - SYS_PES        # JOIN for ProPesNom; Wave-0 BC-only
  - SAU_CBOR       # JOIN for EspCborDes via SAU_ESP.EspCborCod; confirm wave (likely Wave-1 lookup)
# cross-cutting always required:
  - SAU_LOG        # psau_inc_log called after every INS/UPD/DLT
  - SAU_USU        # session / pisauthorized checks
  - SAU_USUCON     # user-level ACL
  - SAU_PRF        # profile
  - SAU_PRFCON     # profile-level ACL

# Only columns physically stored in SAU_IMP (6 total).
# Source: INSERT cursor T000Z18 (sau_imp_impl.java:3055) + UPDATE T000Z19 + SELECT T000Z2/T000Z3.
# Display-only fields (derived from JOINs) are listed separately below.
fields:
  # --- OWN COLUMNS (persisted in SAU_IMP) ---
  - gx: ImpCod
    name: codigo
    type: int
    pk: true
    nullable: false
    readonly: true     # edtImpCod_Enabled=0 (line 978); edtImpCod_Visible=0 (line 925-926)
    domain: Codigo06
    picture: "ZZZZZZ"
    notes: >
      PK; auto-generated by psau_inc_imp (sau_imp_impl.java:1939-1943).
      psau_inc_imp uses MAX(ImpCod)+1 — race condition risk; confirm whether a PostgreSQL
      SEQUENCE exists on the live DB before choosing @GeneratedValue strategy.

  - gx: ImpDat
    name: dataCadastro
    type: date
    pk: false
    nullable: true     # null-guard flag in cursor binding; DB column is nullable
    domain: Data
    picture: "99/99/9999"
    notes: >
      Defaults to server date on INS when blank (sau_imp_impl.java:1007-1011).
      Mandatory via user rule R5 ("Informe a data de cadastro do Impedimento!");
      DB column nullable but service rejects null.

  - gx: ImpDatIni
    name: dataInicio
    type: date
    pk: false
    nullable: true
    domain: Data
    picture: "99/99/9999"
    notes: Period start. Mandatory per R13 ("Informe a data inicial do período de Impedimento!").

  - gx: ImpDatFim
    name: dataFim
    type: date
    pk: false
    nullable: true
    domain: Data
    picture: "99/99/9999"
    notes: Period end. Mandatory per R14 ("Informe a data final do período de Impedimento!").

  - gx: ProPesCod
    name: profissionalCodigo
    type: long
    len: 14
    pk: false
    nullable: true
    domain: Codigo14
    picture: "ZZZZZZZZZZZZZZ"
    notes: >
      FK → SAU_PRO.ProPesCod (Wave-4, not yet migrated). Kept as raw Long @Column.
      Promote to @ManyToOne when SAU_PRO is migrated.
      Mandatory per R7 ("Informe o código do Profissional!").
      Validate against SAU_PRO+SYS_PES (R8) and SAU_PROESP (R11) at service layer.

  - gx: EspCod
    name: especialidade
    type: int            # @ManyToOne join column (SAU_ESP already migrated)
    len: 6
    pk: false
    nullable: true
    domain: Codigo06
    fk: { ref: SAU_ESP, relationship: ManyToOne, fetch: LAZY }
    notes: >
      FK → SAU_ESP.EspCod; SAU_ESP is the reference slice (already migrated).
      Map as @ManyToOne(fetch=LAZY, optional=true) @JoinColumn(name="EspCod").
      Mandatory per R9 ("Informe o código da Especialidade!").

  # --- DISPLAY-ONLY FIELDS (from JOINs — not stored in SAU_IMP; appear in DTOs only) ---
  - gx: ProPesNom
    name: profissionalNome
    type: string
    len: 50
    stored: false
    source_table: SYS_PES   # PesNom WHERE PesCod = ProPesCod
    readonly: true
    picture: "@!"
    notes: >
      Populated by cursor T000Z12 (sau_imp_impl.java:3049) on gxload_17.
      Never sent in write request bodies; resolved at read time via JOIN.

  - gx: ProSit
    name: profissionalSituacao
    type: byte
    len: 1
    stored: false
    source_table: SAU_PRO   # ProSit WHERE ProPesCod = ProPesCod
    readonly: true
    notes: >
      Loaded via cursor T000Z4/T000Z10 (sau_imp_impl.java:1087-1089).
      Passed as hidden field "PROSIT" between form roundtrips; confirm whether
      it restricts create/update for inactive professionals (OQ7).

  - gx: EspNom
    name: especialidadeNome
    type: string
    len: 50
    stored: false
    source_table: SAU_ESP
    readonly: true
    picture: "@!"
    notes: Populated via cursor T000Z13 / gxload_15 (sau_imp_impl.java:3050).

  - gx: EspCborCod
    name: cboCode
    type: string
    len: 6
    stored: false
    source_table: SAU_ESP    # EspCborCod WHERE EspCod = EspCod
    readonly: true
    domain: Ocupacao
    picture: "@!"
    notes: >
      CBO code inherited from specialty. edtEspCborCod_Enabled=0 (line 2265).
      Populated together with EspNom.

  - gx: EspCborDes
    name: cboDescricao
    type: string
    stored: false
    source_table: SAU_CBOR   # CborDes WHERE CborCod = EspCborCod
    readonly: true
    notes: Loaded via cursor T000Z8/T000Z9 / gxload_18 (sau_imp_impl.java:3045).

  - gx: ProPesNomSoundex
    name: profissionalNomeFonetico
    type: string
    len: 50
    stored: false
    source_table: unknown    # SYS_PES or SAU_PRO — confirm via DB introspection (OQ2)
    notes: >
      Phonetic index used as list search filter (hwwsau_imp_impl.java:~640-667).
      Computed by psau_soundex; REST list endpoint should accept ?nomeFonetico= param.

keys:
  pk: { columns: [ImpCod], type: int, generated_by: psau_inc_imp }
  unique: []
  foreign_keys:
    - columns: [EspCod]
      references: SAU_ESP(EspCod)
      nullable: true
      db_constraint: true    # SAU_ESP already migrated; safe to enforce
    - columns: [ProPesCod]
      references: SAU_PRO(ProPesCod)
      nullable: true
      db_constraint: false   # Omit until SAU_PRO (Wave-4) is live; see OQ4

endpoints:
  - method: GET
    path: /api/impedimentos
    desc: "List / search impedimentos with optional filters"
    from: hwwsau_imp_impl.java
    filters:
      - { name: profissionalId,    gx_var: AV5ProPesCod,   type: long }
      - { name: profissionalNome,  gx_var: AV14ProPesNom,  type: string, note: "LIKE prefix + SOUNDEX" }
      - { name: especialidadeId,   type: int }
      - { name: dataInicioFrom,    gx_var: AV10ImpDatIni,  type: date }
      - { name: dataFimUntil,      gx_var: AV9ImpDatFim,   type: date }
    pagination: true
    sort: "ImpCod ASC"      # ORDER BY in cursor T000Z25 (sau_imp_impl.java:3062)

  - method: GET
    path: /api/impedimentos/{id}
    desc: "Read-only detail of one impedimento including joined professional + specialty + CBO"
    from: hviewsau_imp_impl.java

  - method: POST
    path: /api/impedimentos
    desc: "Create new impedimento; ImpCod is server-assigned"
    from: sau_imp INS mode
    notes: >
      Client must NOT send ImpCod. Validates R5-R14 before INSERT.
      Writes audit record via common AuditService after commit (R22).

  - method: PUT
    path: /api/impedimentos/{id}
    desc: "Update existing impedimento"
    from: sau_imp UPD mode
    notes: >
      Optimistic concurrency: re-fetch before update; reject with 409 if any
      of (ImpDat, ImpDatIni, ImpDatFim, ProPesCod, EspCod) changed since load (R20).

  - method: DELETE
    path: /api/impedimentos/{id}
    desc: "Delete impedimento (single-step at API layer; confirmation is frontend-only)"
    from: sau_imp DLT mode
    notes: "GeneXus two-phase DLT is a form pattern; REST DELETE is idempotent (R24)."

  # No /lookup endpoint — SAU_IMP has no hprompt* file.
  # The form uses hpromptsau_pro and hpromptsau_proesp (owned by those slices).

# No patient/PHI data in SAU_IMP. Fields are: system ID, three dates, professional code, specialty code.
# ProPesNom is staff personal data (LGPD Art.7 general, not Art.11 sensitive health data).
# Confirm classification with DPO before cutover.
phi_fields: []

auth:
  mechanism: pisauthorized
  permission_key: "SAU_IMP"    # sau_imp_impl.java:908 — pisauthorized("SAU_IMP", Gx_mode)
  modes: [INS, UPD, DLT, DSP]
  notes: >
    Map to Spring @PreAuthorize: INS→impedimento:create, UPD→impedimento:update,
    DLT→impedimento:delete, DSP→impedimento:read.
    Superuser flag (UsuSysmar=true) bypasses all checks (pisauthorized.java:59-238).
    Confirm role granularity with Wave-0 auth team (OQ5).

parity:
  legacy_base: "http://<host>/ReceituarioJavaEnvironment/servlet"
  scenarios:
    - { id: P1,  desc: "List all impedimentos (default, no filters) — compare count and first-page rows" }
    - { id: P2,  desc: "Filter by profissionalId — results match" }
    - { id: P3,  desc: "Filter by profissionalNome partial — LIKE + SOUNDEX results match" }
    - { id: P4,  desc: "Filter by dataInicio and dataFim range" }
    - { id: P5,  desc: "View detail by known ImpCod — all fields including joined names match" }
    - { id: P6,  desc: "Insert valid record — verify ImpCod generated, appears in list" }
    - { id: P7,  desc: "Insert with missing ImpDat — expect validation error" }
    - { id: P8,  desc: "Insert with invalid ProPesCod (not in SAU_PRO) — expect FK error" }
    - { id: P9,  desc: "Insert with ProPesCod/EspCod pair not in SAU_PROESP — expect FK error" }
    - { id: P10, desc: "Update — change dates; verify optimistic concurrency succeeds when data unchanged" }
    - { id: P11, desc: "Update — simulate concurrent change; verify 409-like rejection" }
    - { id: P12, desc: "Delete — single-step, record gone from list" }

status: verified   # parity 22/22 PARITY 2026-06-22. Report: backend/src/test/resources/parity/impedimento/PARITY-REPORT.md.
# Verified vs live shared snapshot + high-confidence mined rules (legacy WW is auth-gated → no headless HTML capture).
# BUG FOUND & FIXED: date-range filter 500'd (PSQLException 42P18, untyped `:param IS NULL` on a date param) —
#   fixed by CAST(:param AS DATE) in all 4 query bodies; regression test ImpedimentoControllerIT#filtersByDateRange.
#   Suite green: ControllerIT 13/13, ServiceTest 12/12.
# OQ3 RESOLVED (no ImpCod sequence on live DB → MAX+1 correct), OQ12 RESOLVED (SAU_PRO PK=propescod), OQ7 RESOLVED
#   (no prescription enforcement), OQ1 RESOLVED (SAU_CBOR live). Cutover notes (NOT parity blockers): RD1 optimistic
#   locking deferral (P11), OQ2 SOUNDEX→LIKE search difference, OQ4 FK deferred to SAU_PRO/Wave-4, OQ5/6/8/9 product-
#   owner/auth confirmations (parity holds either way), R22 audit persistence = Wave-0. Required-field rejects via 400
#   (Bean Validation) not legacy message — RF6 status nuance.

open_questions:
  - id: OQ1
    topic: "SAU_CBOR wave assignment"
    desc: >
      SAU_CBOR (CBO classification) appears in TableAccess XML and JOIN cursors but is not listed
      in BACKLOG.md waves. It is likely a small lookup (Wave-1 class). Add to backlog and migrate
      before SAU_IMP integration tests can run the EspCborDes derivation.

  - id: OQ2
    topic: "ProPesNomSoundex storage location"
    status: RESOLVED (2026-06-22 via SAU_PRO extraction) — ProPesNomSoundex IS a stored column on
            SAU_PRO (varchar(50)), computed from SYS_PES.PesNom via psau_soundex. BUT the legacy
            work-with name search (hwwsau_pro_impl.java:2493-2520) filters PesNom with a literal
            `LIKE %?%`, NOT the soundex column (which is display-only). So SAU_IMP's LIKE-based
            profissionalNome filter is FAITHFUL to legacy, not a divergence. No phonetic search to port.
    desc: >
      A851ProPesNomSoundex is used as a search filter (hwwsau_imp_impl.java:~640-667), populated
      by psau_soundex and passed into dynamic SQL. Confirm whether this is a column on SYS_PES,
      SAU_PRO, or a computed value via DB introspection before implementing the ?nomeFonetico= filter.

  - id: OQ3
    topic: "ImpCod PK generation — sequence vs MAX+1"
    status: RESOLVED (2026-06-22) — NO sequence on live DB (only unrelated esus_importacao_* seq).
            Service uses repo.findMaxCodigo()+1 (matches legacy psau_inc_imp); race handled by PK + R21→409.
            A @GeneratedValue(SEQUENCE) would 500 on the live DB. Confirmed by parity P6 (ImpCod auto-assigned).
    desc: >
      psau_inc_imp.java:54-72 uses SELECT MAX(ImpCod) ... LIMIT 1 + 1. This is a race condition
      under concurrent inserts. Confirm with DBA whether a PostgreSQL SEQUENCE exists for ImpCod.
      If not, create one in the Flyway baseline; use @GeneratedValue(SEQUENCE) in the entity.

  - id: OQ4
    topic: "FK cycle: SAU_IMP (Wave-2) → SAU_PRO (Wave-4)"
    desc: >
      ProPesCod FK constraint is omitted from the DDL until SAU_PRO is live. Decide whether to use
      DEFERRABLE INITIALLY DEFERRED or simply add a separate Flyway migration after SAU_PRO cutover.

  - id: OQ5
    topic: "Auth permission granularity"
    desc: >
      pisauthorized uses object key "SAU_IMP". Confirm with Wave-0 auth team whether Spring Boot
      should use transaction-level permissions (SAUDE_IMP) or a coarser role (SAUDE_CADASTRO),
      and what hwwsau_imp requires separately.

  - id: OQ6
    topic: "ProSit role in business rules"
    desc: >
      ProSit (professional status from SAU_PRO) is loaded on every read and passed as hidden field
      PROSIT. No explicit conditional on it was found in sau_imp_impl.java. Confirm with product
      owner whether inactive professionals (ProSit='I') should be blocked from new impedimentos.

  - id: OQ7
    topic: "SAU_IMP references in prescription flow"
    status: RESOLVED (2026-06-22) — NO prescription enforcement.
    desc: >
      RESOLVED: full-tree search (LC_ALL=C grep -a — legacy files are ISO-8859, the default ugrep
      skips them as binary without -a) found the prescription constellation (sau_recesp_impl.java,
      hwwsau_recesp_impl.java, hviewsau_recesp_impl.java, hsau_recespgeneral_impl.java,
      rsau_recesp_impl.java) has ZERO references to SAU_IMP/ImpCod/Impedimento. An active impediment
      does NOT block prescriptions in legacy — SAU_IMP is informational-only, no downstream
      enforcement to preserve at cutover.
      The ONLY external references to SAU_IMP are:
        (a) hwwsau_pro_impl.java — a UI nav button "Consultar Impedimentos" (no business rule); and
        (b) sau_proesp_impl.java:5462 (cursor T000Y26) — a REVERSE delete-guard:
            SELECT ImpCod FROM SAU_IMP WHERE ProPesCod=? AND EspCod=? LIMIT 1 → CannotDeleteReferencedRecord.
            i.e. SAU_PROESP cannot be deleted while an impediment references its (ProPesCod,EspCod).
            → This is a rule on SAU_PROESP (Wave-4), to capture in THAT slice's spec, not here.
      Cutover of SAU_IMP is therefore NOT gated on any prescription-flow behavior.

  - id: OQ8
    topic: "Date period cross-validation (ImpDatIni ≤ ImpDatFim)"
    desc: >
      No ImpDatFim >= ImpDatIni cross-validation exists in the GeneXus code. Confirm with product
      owner whether this is an intentional omission or a missing rule to add in Spring Boot.

  - id: OQ9
    topic: "Overlapping impediment periods"
    desc: >
      No uniqueness constraint exists on (ProPesCod, EspCod) with overlapping dates. Multiple
      simultaneous impedimentos for the same professional/specialty appear to be permitted.
      Confirm with product owner whether overlapping periods should be rejected.

  - id: OQ10
    topic: "SAU_LOG logProPesCod hardcoded to 0"
    desc: >
      psau_inc_log is called with logProPesCod=0 and logNomeProfissional='' even though the
      impeded professional is known. Confirm whether the audit record should capture the professional
      code for traceability, or if the zero/empty behavior must be preserved for existing reports.

  - id: OQ11
    topic: "hviewsau_imp vs hsau_impgeneral — two panels, one endpoint?"
    desc: >
      Both share the description 'Informações do Impedimento do Profissional'. Confirm whether
      they serve different layout contexts (embedded vs. standalone) to decide if one
      GET /api/impedimentos/{id} response shape suffices or two DTOs are needed.

  - id: OQ12
    topic: "SAU_PRO PK column name — ProPesCod vs ProCod"
    status: RESOLVED (2026-06-22) — live SAU_PRO PK column is `propescod` (BIGINT), matching all cursors.
            V1 baseline already corrected (ProCod INTEGER → ProPesCod BIGINT). FK queries verified live.
    desc: >
      V1__baseline.sql has a SAU_PRO stub with ProCod as PK, but all cursors in sau_imp_impl.java
      reference ProPesCod. Confirm the live DB column name before writing the FK migration.
```

---

## JPA Entity Sketch

```java
@Entity
@Table(name = "SAU_IMP")
public class Impedimento {

    @Id
    @Column(name = "ImpCod", nullable = false)
    // TODO: confirm PK strategy — MAX+1 in psau_inc_imp; ideally replace with SEQUENCE
    private Integer codigo;

    @Column(name = "ImpDat")
    private LocalDate dataCadastro;

    @Column(name = "ImpDatIni")
    private LocalDate dataInicio;

    @Column(name = "ImpDatFim")
    private LocalDate dataFim;

    @Column(name = "ProPesCod")
    private Long profissionalCodigo;     // raw FK — SAU_PRO not yet migrated (Wave-4)

    @ManyToOne(fetch = FetchType.LAZY, optional = true)
    @JoinColumn(name = "EspCod")
    private Especialidade especialidade; // FK → SAU_ESP (already migrated ✅)
}
```

---

## Flyway DDL

**Done — appended to `V1__baseline.sql` on 2026-06-21.** All column types and nullability confirmed
against live `saude-mandaguari` DB. SAU_PRO and SAU_PROESP stubs in V1 were also corrected
(`ProCod INTEGER` → `ProPesCod BIGINT`) in the same session. SAU_CBOR corrected to `CHAR(6)`.
No separate forward migration needed for this slice.

---

## Rules

> 26 rules extracted from `sau_imp_impl.java` and `psau_inc_imp.java`.
> Source citations are exact line numbers. Confidence: high=clear validation, medium=inferred.

```yaml
rules:

  # ── AUTHORIZATION ────────────────────────────────────────────────────────────

  - id: R1
    when: always
    kind: authorization
    desc: >
      Every request must carry a valid GX_SESSION_ID cookie (sau_imp_impl.java:203-208).
      Replace with JWT bearer-token validation on every endpoint.
    source: "sau_imp_impl.java:203-208"
    confidence: high
    test: ImpedimentoControllerIT#rejectsRequestWithoutSessionToken

  - id: R2
    when: always
    kind: authorization
    desc: >
      On Start event, pisauthorized("SAU_IMP", <mode>) is called. Checks SAU_USU/SAU_USUCON/
      SAU_PRF/SAU_PRFCON for INS/UPD/DLT/DSP access. Superuser (UsuSysmar=true) bypasses all.
      Unauthorized → redirect to hnotauthorizedprompt. Spring Boot: @PreAuthorize per mode.
    source: "sau_imp_impl.java:908-923, pisauthorized.java:59-238"
    confidence: medium
    test: ImpedimentoServiceTest#deniesUnauthorizedUser

  - id: R3
    when: always
    kind: authorization
    desc: >
      IntegratedSecurityEnabled() returns false; GeneXus built-in security is disabled.
      All authorization goes through pisauthorized (R2).
    source: "sau_imp.java:33-36, sau_imp_impl.java:2527-2530"
    confidence: high
    test: ImpedimentoControllerIT#customAuthorizationIsUsedNotBuiltIn

  # ── AUTO-GENERATED PRIMARY KEY ────────────────────────────────────────────────

  - id: R4
    when: insert
    kind: default
    desc: >
      ImpCod is auto-generated by psau_inc_imp (MAX(ImpCod)+1). Assigned in afterConfirm0Z46
      before INSERT. Field is hidden and not editable on the form. Client must never send ImpCod.
    source: "sau_imp_impl.java:1933-1943, psau_inc_imp.java:54-72"
    confidence: high
    test: ImpedimentoServiceTest#autoGeneratesImpCodOnInsert

  # ── REQUIRED FIELD – REGISTRATION DATE ───────────────────────────────────────

  - id: R5
    when: insert|update
    kind: validation
    desc: >
      ImpDat must be a valid calendar date and not null.
      Error: "Informe a data de cadastro do Impedimento!"
    source: "sau_imp_impl.java:563-578, sau_imp_impl.java:1068-1074"
    confidence: high
    test: ImpedimentoServiceTest#rejectsNullOrInvalidImpDat

  - id: R6
    when: insert
    kind: default
    desc: "ImpDat defaults to the current server date when not supplied (Gx_mode=INS, Gx_BScreen=0)."
    source: "sau_imp_impl.java:1007-1012"
    confidence: high
    test: ImpedimentoServiceTest#defaultsImpDatToTodayOnInsert

  # ── REQUIRED FIELD – PROFESSIONAL ────────────────────────────────────────────

  - id: R7
    when: insert|update
    kind: validation
    desc: >
      ProPesCod must not be zero.
      Error: "Informe o código do Profissional!"
    source: "sau_imp_impl.java:1119-1125, sau_imp_impl.java:580-596"
    confidence: high
    test: ImpedimentoServiceTest#rejectsZeroProPesCod

  - id: R8
    when: insert|update
    kind: validation
    desc: >
      ProPesCod must exist in SAU_PRO (cursor T000Z4) and SYS_PES (cursor T000Z7).
      Error: "Não existe 'Profissionais'."
    source: "sau_imp_impl.java:1076-1118"
    confidence: high
    test: ImpedimentoServiceTest#rejectsNonExistentProPesCod

  # ── REQUIRED FIELD – ESPECIALIDADE ───────────────────────────────────────────

  - id: R9
    when: insert|update
    kind: validation
    desc: >
      EspCod must not be zero.
      Error: "Informe o código da Especialidade!"
    source: "sau_imp_impl.java:1158-1164"
    confidence: high
    test: ImpedimentoServiceTest#rejectsZeroEspCod

  - id: R10
    when: insert|update
    kind: validation
    desc: >
      EspCod must exist in SAU_ESP (cursor T000Z5).
      Error: "Não existe 'Especialidades'."
      On success, EspNom and EspCborCod are loaded into the display context.
    source: "sau_imp_impl.java:1127-1144"
    confidence: high
    test: ImpedimentoServiceTest#rejectsNonExistentEspCod

  # ── FK – PROFESSIONAL'S ESPECIALIDADE (SAU_PROESP) ───────────────────────────

  - id: R11
    when: insert|update
    kind: validation
    desc: >
      The pair (ProPesCod, EspCod) must exist in SAU_PROESP (cursor T000Z6).
      The professional must have the given specialty assigned.
      Error: "Não existe 'Especialidade do Profissional'."
    source: "sau_imp_impl.java:1091-1102"
    confidence: high
    test: ImpedimentoServiceTest#rejectsEspCodNotBelongingToProfissional

  # ── FK – CBO (SAU_CBOR) ───────────────────────────────────────────────────────

  - id: R12
    when: insert|update
    kind: validation
    desc: >
      If EspCborCod (derived from SAU_ESP) is non-empty, it must exist in SAU_CBOR (cursor T000Z8).
      Error message reads "Não existe 'SAU_ESP'." — likely a legacy GeneXus-generated text;
      use a clearer message in Spring Boot (see OQ-rule2).
    source: "sau_imp_impl.java:1145-1157"
    confidence: medium
    test: ImpedimentoServiceTest#rejectsInvalidCborCod

  # ── REQUIRED FIELDS – PERIOD DATES ───────────────────────────────────────────

  - id: R13
    when: insert|update
    kind: validation
    desc: >
      ImpDatIni must not be null.
      Error: "Informe a data inicial do período de Impedimento!"
    source: "sau_imp_impl.java:623-639, sau_imp_impl.java:1165-1171"
    confidence: high
    test: ImpedimentoServiceTest#rejectsNullImpDatIni

  - id: R14
    when: insert|update
    kind: validation
    desc: >
      ImpDatFim must not be null.
      Error: "Informe a data final do período de Impedimento!"
    source: "sau_imp_impl.java:640-656, sau_imp_impl.java:1172-1178"
    confidence: high
    test: ImpedimentoServiceTest#rejectsNullImpDatFim

  # ── DERIVED / READ-ONLY FIELDS ────────────────────────────────────────────────

  - id: R15
    when: load
    kind: derivation
    desc: >
      ProPesNom (professional name) is derived from SYS_PES.PesNom WHERE PesCod = ProPesCod
      via cursor T000Z12 (gxload_17). Never stored in SAU_IMP; resolved at read time via JOIN.
    source: "sau_imp_impl.java:1251-1278"
    confidence: high
    test: ImpedimentoServiceTest#derivesProPesNomFromSysPes

  - id: R16
    when: load
    kind: derivation
    desc: >
      EspNom is derived from SAU_ESP.EspNom WHERE EspCod = EspCod via cursor T000Z13 (gxload_15).
      Not stored.
    source: "sau_imp_impl.java:1281-1312"
    confidence: high
    test: ImpedimentoServiceTest#derivesEspNomFromSauEsp

  - id: R17
    when: load
    kind: derivation
    desc: >
      EspCborCod is derived from SAU_ESP.EspCborCod WHERE EspCod = EspCod.
      Always read-only (edtEspCborCod_Enabled=0). Not stored.
    source: "sau_imp_impl.java:2264-2265"
    confidence: high
    test: ImpedimentoServiceTest#derivesEspCborCodFromSauEsp

  - id: R18
    when: load
    kind: derivation
    desc: >
      EspCborDes is derived from SAU_CBOR.CborDes WHERE CborCod = EspCborCod via cursor T000Z8.
      Not stored.
    source: "sau_imp_impl.java:1314-1339"
    confidence: high
    test: ImpedimentoServiceTest#derivesEspCborDesFromSauCbor

  - id: R19
    when: load
    kind: derivation
    desc: >
      ProSit (professional status from SAU_PRO) is loaded via cursor T000Z4 and passed as
      hidden field PROSIT between roundtrips. Not stored in SAU_IMP. Confirm whether it blocks
      any business operations (OQ6).
    source: "sau_imp_impl.java:1087-1089, sau_imp_impl.java:2071"
    confidence: medium
    test: ImpedimentoServiceTest#derivesProSitFromSauPro

  # ── OPTIMISTIC CONCURRENCY ────────────────────────────────────────────────────

  - id: R20
    when: update|delete
    kind: validation
    desc: >
      Before committing, re-read SAU_IMP FOR UPDATE NOWAIT. If locked (status 103): error
      "SAU_IMP está bloqueado". If any of (ImpDat, ImpDatIni, ImpDatFim, ProPesCod, EspCod)
      changed since form load (Z-snapshot mismatch): error "SAU_IMP foi alterado" → HTTP 409.
    source: "sau_imp_impl.java:1581-1630"
    confidence: high
    test: ImpedimentoServiceTest#rejectsConcurrentModification

  # ── DUPLICATE PK ──────────────────────────────────────────────────────────────

  - id: R21
    when: insert
    kind: validation
    desc: >
      If INSERT returns status 1 (duplicate PK), raise "Não foi possível inserir o registro".
      In practice this should not occur with proper sequence-based PK generation.
      Map to HTTP 409 DataIntegrityViolationException handler.
    source: "sau_imp_impl.java:1654-1658"
    confidence: high
    test: ImpedimentoServiceTest#rejectsDuplicateImpCod

  # ── AUDIT ─────────────────────────────────────────────────────────────────────

  - id: R22
    when: insert|update|delete
    kind: audit
    desc: >
      After every successful commit, psau_inc_log writes to SAU_LOG:
      LogOpe=<INS|UPD|DLT>, LogTab="SAU_IMP", LogKey=<ImpCod>.
      In Spring Boot: emit via common AuditService — entity=IMPEDIMENTO, operation=<mode>,
      entityKey=<ImpCod>, performedBy=<userId>, timestamp=<now>.
      Note: legacy hardcodes logProPesCod=0 and logNomeProfissional='' (see OQ10).
    source: "sau_imp_impl.java:1853-1883, psau_inc_log.java:88-115"
    confidence: high
    test: ImpedimentoServiceTest#writesAuditLogOnEveryMutation

  # ── SIDE-EFFECTS ──────────────────────────────────────────────────────────────

  - id: R23
    when: insert|update|delete
    kind: side-effect
    desc: >
      After Trn event (e120Z2) redirects to hwwsau_imp (list screen).
      In Spring Boot: REST returns created/updated/deleted resource; frontend navigates to list.
      No additional business logic in After Trn.
    source: "sau_imp_impl.java:932-937"
    confidence: high
    test: ImpedimentoControllerIT#redirectsToListAfterSuccessfulMutation

  - id: R24
    when: delete
    kind: side-effect
    desc: >
      GeneXus DLT uses a two-phase confirm. In Spring Boot REST, DELETE is single-step;
      confirmation is a frontend concern only. The API layer requires no two-step handshake.
    source: "sau_imp_impl.java:868-888, sau_imp_impl.java:1799-1831"
    confidence: high
    test: ImpedimentoControllerIT#deleteRequiresNoServerConfirmationStep

  # ── FORM FIELD CONSTRAINTS (→ DTO constraints) ────────────────────────────────

  - id: R25
    when: load
    kind: derivation
    desc: >
      ImpCod is always hidden (edtImpCod_Visible=0) and disabled (edtImpCod_Enabled=0).
      Must never be accepted as a user-supplied field in POST/PUT request bodies.
    source: "sau_imp_impl.java:925-926, sau_imp_impl.java:978-987, sau_imp_impl.java:2281"
    confidence: high
    test: ImpedimentoControllerIT#ignoresClientSuppliedImpCodOnCreate

  - id: R26
    when: load
    kind: derivation
    desc: >
      ProPesNom, EspNom, EspCborCod are always read-only (edtXxx_Enabled=0).
      Must not be accepted in write request bodies.
    source: "sau_imp_impl.java:2265-2273"
    confidence: high
    test: ImpedimentoControllerIT#ignoresReadOnlyDerivedFieldsOnWrite
```
